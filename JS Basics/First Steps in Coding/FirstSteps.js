 function convertor(input) {

    let usd = Number(input[1]);
    let bgn = Number(usd * 1.79549);
    console.log(bgn);
    //console.table (input);
}

convertor ([22, 100, 12.5]) 





function namesLists(input) {

     let name = (input[1]);
     let name2 = (input[2]);
     console.log (name + " and " + name2);
}

namesLists (["George", "Amanda"]);