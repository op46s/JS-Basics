function repainting (input) {

    let nylon = Number(input([0])) * 1.5;
    let paint = Number(input([1])) * 14.5;
    let thinner = Number(input([2])) * 5.0;
    let bag = 0.4;

        console.log (paint * 1.1);

}